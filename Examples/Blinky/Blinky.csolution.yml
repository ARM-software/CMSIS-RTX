solution:
  created-for: CMSIS-Toolbox@2.10.0
  cdefault:

  select-compiler:
    - compiler: AC6
    - compiler: GCC
    - compiler: IAR
    - compiler: CLANG

  target-types:
    - type: FVP
      device: ARMCM3

  build-types:
    - type: Debug
      debug: on
      optimize: none

    - type: Release
      debug: off
      optimize: balanced

  projects:
    - project: Blinky.cproject.yml
